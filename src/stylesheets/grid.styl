$gutter = 1rem

.row
  display: flex
  flex-direction: column
  @media (min-width: 600px)
    flex-direction: row
    margin-left: -($gutter)
    margin-right: -($gutter)
  .col-sm-6
    @media (min-width: 600px)
      width 50%
      padding-right: $gutter
      padding-left: $gutter
