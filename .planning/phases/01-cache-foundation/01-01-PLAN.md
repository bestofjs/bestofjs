---
phase: 01-cache-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/repo-trends.ts
  - packages/db/src/schema/project-trends.ts
  - packages/db/src/schema/index.ts
autonomous: true
requirements: [CACHE-01, CACHE-02, DATA-03]

must_haves:
  truths:
    - "repo_trends table exists in the database with star counts, 5 trend delta columns, popularity_score, and activity_score"
    - "project_trends table exists in the database with package_name, monthly_downloads, usage_score, and relevance_score"
    - "repo_trends is keyed by repo_id (not project_id) to handle monorepo siblings correctly"
    - "Deleting a repo cascades to delete its repo_trends row"
    - "Deleting a project cascades to delete its project_trends row"
  artifacts:
    - path: "packages/db/src/schema/repo-trends.ts"
      provides: "repo_trends Drizzle table definition with relations and indexes"
      exports: ["repoTrends", "repoTrendsRelations"]
    - path: "packages/db/src/schema/project-trends.ts"
      provides: "project_trends Drizzle table definition with relations and indexes"
      exports: ["projectTrends", "projectTrendsRelations"]
    - path: "packages/db/src/schema/index.ts"
      provides: "Barrel re-exports of new schema files"
      contains: "repo-trends"
  key_links:
    - from: "packages/db/src/schema/repo-trends.ts"
      to: "packages/db/src/schema/repos.ts"
      via: "foreign key reference"
      pattern: 'references.*repos\.id'
    - from: "packages/db/src/schema/project-trends.ts"
      to: "packages/db/src/schema/projects.ts"
      via: "foreign key reference"
      pattern: 'references.*projects\.id'
    - from: "packages/db/src/schema/index.ts"
      to: "packages/db/src/schema/repo-trends.ts"
      via: "barrel export"
      pattern: 'export.*repo-trends'
---

<objective>
Define Drizzle schema for `repo_trends` and `project_trends` cache tables and generate the database migration.

Purpose: These tables are the foundation for all subsequent work -- scoring functions write to them, listing queries read from them.
Output: Two schema files, updated barrel export, generated migration SQL.
</objective>

<execution_context>
@/home/ubuwarudo/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuwarudo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cache-foundation/01-CONTEXT.md
@.planning/phases/01-cache-foundation/01-RESEARCH.md

@packages/db/src/schema/repos.ts
@packages/db/src/schema/projects.ts
@packages/db/src/schema/index.ts

<interfaces>
<!-- Existing schema patterns to follow exactly -->

From packages/db/src/schema/repos.ts:
```typescript
export const repos = pgTable("repos", {
  id: text("id").primaryKey(),
  stars: integer("stargazers_count"),
  last_commit: timestamp("last_commit"),
  contributor_count: integer("contributor_count"),
  // ...
});
```

From packages/db/src/schema/projects.ts:
```typescript
export const projects = pgTable("projects", {
  id: text("id").primaryKey(),
  repoId: text("repoId").references(() => repos.id, { onDelete: "cascade" }).notNull(),
  // ...
});
```

From packages/db/src/schema/index.ts (barrel pattern):
```typescript
export * from "./repos";
export * from "./projects";
// etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define repo_trends and project_trends Drizzle schemas</name>
  <files>packages/db/src/schema/repo-trends.ts, packages/db/src/schema/project-trends.ts, packages/db/src/schema/index.ts</files>
  <action>
Create `packages/db/src/schema/repo-trends.ts`:
- Import `relations` from `drizzle-orm`, `index`, `integer`, `pgTable`, `text`, `timestamp` from `drizzle-orm/pg-core`, and `repos` from `./repos`
- Define `repoTrends = pgTable("repo_trends", {...}, (table) => [...])`
- Columns (per CONTEXT.md locked decisions):
  - `repoId: text("repo_id").primaryKey().references(() => repos.id, { onDelete: "cascade" })`
  - `stars: integer("stars")` (nullable -- current star count, denormalized)
  - `daily: integer("daily")` (nullable -- NULL = cannot compute, 0 = computed zero)
  - `weekly: integer("weekly")` (nullable)
  - `monthly: integer("monthly")` (nullable)
  - `quarterly: integer("quarterly")` (nullable)
  - `yearly: integer("yearly")` (nullable)
  - `popularityScore: integer("popularity_score").notNull().default(0)`
  - `activityScore: integer("activity_score").notNull().default(0)`
  - `refreshedAt: timestamp("refreshed_at").notNull().defaultNow()`
- Indexes (third arg callback returning array):
  - `index("repo_trends_stars_idx").on(table.stars.desc())`
  - `index("repo_trends_daily_idx").on(table.daily.desc().nullsLast())`
  - `index("repo_trends_weekly_idx").on(table.weekly.desc().nullsLast())`
  - `index("repo_trends_monthly_idx").on(table.monthly.desc().nullsLast())`
  - `index("repo_trends_quarterly_idx").on(table.quarterly.desc().nullsLast())`
  - `index("repo_trends_yearly_idx").on(table.yearly.desc().nullsLast())`
  - `index("repo_trends_popularity_idx").on(table.popularityScore.desc())`
  - `index("repo_trends_activity_idx").on(table.activityScore.desc())`
- Define `repoTrendsRelations = relations(repoTrends, ({ one }) => ({ repo: one(repos, { fields: [repoTrends.repoId], references: [repos.id] }) }))`

Create `packages/db/src/schema/project-trends.ts`:
- Import `relations` from `drizzle-orm`, `index`, `integer`, `pgTable`, `text`, `timestamp` from `drizzle-orm/pg-core`, and `projects` from `./projects`
- Define `projectTrends = pgTable("project_trends", {...}, (table) => [...])`
- Columns:
  - `projectId: text("project_id").primaryKey().references(() => projects.id, { onDelete: "cascade" })`
  - `packageName: text("package_name")` (nullable -- NULL for projects without packages)
  - `monthlyDownloads: integer("monthly_downloads")` (nullable)
  - `usageScore: integer("usage_score").notNull().default(0)`
  - `relevanceScore: integer("relevance_score").notNull().default(0)`
  - `refreshedAt: timestamp("refreshed_at").notNull().defaultNow()`
- Indexes:
  - `index("project_trends_usage_idx").on(table.usageScore.desc())`
  - `index("project_trends_relevance_idx").on(table.relevanceScore.desc())`
  - `index("project_trends_downloads_idx").on(table.monthlyDownloads.desc().nullsLast())`
- Define `projectTrendsRelations = relations(projectTrends, ({ one }) => ({ project: one(projects, { fields: [projectTrends.projectId], references: [projects.id] }) }))`

Update `packages/db/src/schema/index.ts`:
- Add `export * from "./repo-trends";` and `export * from "./project-trends";` to the barrel exports

IMPORTANT: Score columns are INTEGER NOT NULL DEFAULT 0 per locked decision. Trend delta columns are nullable INTEGER (no `.notNull()`). Do NOT use `real()` -- use `integer()` for all score columns per CONTEXT.md.
  </action>
  <verify>
    <automated>cd /home/ubuwarudo/Project/PERSONAL/dev/bestofjs && pnpm --filter @repo/db exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Both schema files export pgTable definitions with correct column types, indexes with desc/nullsLast, relations, and are re-exported from the barrel index. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and verify Drizzle migration</name>
  <files>packages/db/drizzle/</files>
  <action>
Run the Drizzle Kit migration generator:
```bash
cd /home/ubuwarudo/Project/PERSONAL/dev/bestofjs && pnpm --filter @repo/db generate
```

This reads the updated schema files and produces a new migration SQL file in `packages/db/drizzle/`. Verify the generated SQL contains:
1. `CREATE TABLE "repo_trends"` with all columns and correct types
2. `CREATE TABLE "project_trends"` with all columns and correct types
3. Foreign key constraints referencing `repos(id)` and `projects(id)` with ON DELETE CASCADE
4. All index definitions with DESC and NULLS LAST where appropriate

If the generated migration looks correct, no manual edits needed. If Drizzle Kit generates incorrect index syntax (unlikely with v0.31.4+), manually adjust the SQL.

Do NOT run `pnpm --filter @repo/db migrate` -- migration application happens at deploy time or manually by the developer. Only generate the migration file.
  </action>
  <verify>
    <automated>cd /home/ubuwarudo/Project/PERSONAL/dev/bestofjs && ls -la packages/db/drizzle/ | tail -5 && cat packages/db/drizzle/$(ls packages/db/drizzle/ | sort | tail -1)/migration.sql 2>/dev/null || cat packages/db/drizzle/$(ls packages/db/drizzle/ | sort | tail -1)/*.sql 2>/dev/null | head -60</automated>
  </verify>
  <done>A new migration file exists in packages/db/drizzle/ containing CREATE TABLE statements for repo_trends and project_trends with correct column types, foreign keys with CASCADE, and descending indexes with NULLS LAST.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd packages/db && pnpm exec tsc --noEmit`
- Schema exports work: `grep -c "repo-trends\|project-trends" packages/db/src/schema/index.ts` returns 2
- Migration file generated: new directory in `packages/db/drizzle/` with SQL
</verification>

<success_criteria>
- repo_trends and project_trends tables are defined in Drizzle schema with exact column types from CONTEXT.md
- repo_trends keyed by repo_id (DATA-03), project_trends keyed by project_id
- All score columns INTEGER NOT NULL DEFAULT 0, all trend columns nullable INTEGER
- Descending indexes with NULLS LAST on trend columns
- Foreign keys with ON DELETE CASCADE
- Migration SQL generated (not applied)
- Barrel export updated
</success_criteria>

<output>
After completion, create `.planning/phases/01-cache-foundation/01-01-SUMMARY.md`
</output>
