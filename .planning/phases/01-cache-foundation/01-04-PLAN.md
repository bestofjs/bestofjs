---
phase: 01-cache-foundation
plan: 04
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - packages/db/package.json
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04, CACHE-05]

must_haves:
  truths:
    - "Backend app can import scoring functions via @repo/db/scores"
  artifacts:
    - path: "packages/db/package.json"
      provides: "Export entry for scores barrel"
      contains: "./scores"
  key_links:
    - from: "packages/db/package.json"
      to: "packages/db/src/scores/index.ts"
      via: "exports map entry"
      pattern: '"./scores".*"./src/scores/index.ts"'
---

<objective>
Add the `"./scores"` export entry to `packages/db/package.json` so the backend app can import scoring functions via `@repo/db/scores`.

Purpose: Without this export, Plan 01-03's refresh task cannot import from `@repo/db/scores`. The scores barrel (created by Plan 01-02) exists but is not exposed as a package entry point.
Output: Updated package.json with one new export line.
</objective>

<execution_context>
@/home/ubuwarudo/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuwarudo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cache-foundation/01-CONTEXT.md

@packages/db/package.json

<interfaces>
<!-- From Plan 01-02 output (scores barrel) -->
From packages/db/src/scores/index.ts (created by Plan 01-02):
```typescript
export { computePopularityScore } from "./popularity";
export { computeActivityScore } from "./activity";
export { computeUsageScore } from "./usage";
export { computeRelevanceScore } from "./relevance";
export { resolvePrimaryPackage } from "./primary-package";
```

<!-- Existing package.json exports pattern -->
From packages/db/package.json (current):
```json
{
  "exports": {
    ".": "./src/index.ts",
    "./drizzle": "./src/drizzle.ts",
    "./schema": "./src/schema.ts",
    "./constants": "./src/constants.ts",
    "./projects": "./src/projects/index.ts",
    "./shared-schemas": "./src/shared-schemas.ts",
    "./snapshots": "./src/snapshots/index.ts",
    "./tags": "./src/tags/index.ts",
    "./hall-of-fame": "./src/hall-of-fame.ts",
    "./types": "./src/types.ts"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scores export to packages/db/package.json</name>
  <files>packages/db/package.json</files>
  <action>
Add one new entry to the `"exports"` object in `packages/db/package.json`:

```json
"./scores": "./src/scores/index.ts"
```

Place it after the `"./types"` entry (last in the current list). This follows the existing pattern where each subdirectory barrel gets its own export path (e.g., `"./snapshots": "./src/snapshots/index.ts"`).

Per CONTEXT.md locked decision: "One new export: `"./scores": "./src/scores/index.ts"` in `packages/db/package.json`". This enables `import { ... } from "@repo/db/scores"` in the backend app.

Do NOT add any other exports -- schema tables use the existing barrel at `"./schema"`.
  </action>
  <verify>
    <automated>cd /home/ubuwarudo/Project/PERSONAL/dev/bestofjs && grep -c '"./scores"' packages/db/package.json</automated>
  </verify>
  <done>packages/db/package.json contains `"./scores": "./src/scores/index.ts"` in the exports map. The backend app can now import from `@repo/db/scores`.</done>
</task>

</tasks>

<verification>
- Export entry exists: `grep "./scores" packages/db/package.json` returns the entry
- JSON is valid: `node -e "JSON.parse(require('fs').readFileSync('packages/db/package.json', 'utf8'))"`
</verification>

<success_criteria>
- packages/db/package.json exports map contains `"./scores": "./src/scores/index.ts"`
- No other exports modified
- JSON remains valid
</success_criteria>

<output>
After completion, create `.planning/phases/01-cache-foundation/01-04-SUMMARY.md`
</output>
